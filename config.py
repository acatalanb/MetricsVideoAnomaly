import os

# Global configuration (easy to change)
IMG_SIZE = 224
SEQ_LEN = 16
BATCH_SIZE = 16

# Default dataset location inside the project
DEFAULT_DATASET_ROOT = r"K:\VideoDataset"
DEFAULT_DATASET_NAME = "kvasir"   # Change to "crime-ucf" if needed